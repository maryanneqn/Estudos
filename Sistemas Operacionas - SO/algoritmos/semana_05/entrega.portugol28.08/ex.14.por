programa {
  caracter letra
  inteiro codigo
  funcao inicio() {
    escreva("Letra do a setor(F,V,M, etc.):")
    leia(letra)
    escreva("Código do funcionário:")
    leia(codigo)
    Escreva("Funcionário do setor", letra, ",código", codigo)
    
  }
}
